services:
  client-service:
    build: 
      context: ./ClientService
      dockerfile: Dockerfile
    ports: 
      - "8001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal;Database=ClientServiceDb;User Id=sa;Password=s3cur3ADMIN2000;Encrypt=false;

  user-service:
    build: 
      context: ./UserService
      dockerfile: Dockerfile
    ports: 
      - "8002:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal;Database=ClientServiceDb;User Id=sa;Password=s3cur3ADMIN2000;Encrypt=false;